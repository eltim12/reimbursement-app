# Local overrides for development only.
# Do NOT put VITE_API_BASE_URL here — .env.local is also loaded during
# production builds and would bake localhost into Firebase hosting.
# Use .env.development / .env.production instead.
